apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: er-efs-pvc
  namespace: er
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: efs-sc
  resources:
    requests:
      storage: 20Gi
  volumeName: er-efs-pv